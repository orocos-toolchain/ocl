DEPENDENT_OPTION( BUILD_APCI_TEST "Build APCI 2200/1032/1710 test for LXRT" ON "BUILD_APCI;OS_LXRT;BUILD_TESTS" OFF )

IF (BUILD_APCI_TEST)

    GLOBAL_ADD_TEST( test2200 test2200.cpp )
    GLOBAL_ADD_TEST( test1032 test1032.cpp )
    GLOBAL_ADD_TEST( test1710SSI test1710SSI.cpp )
    # Use  TARGET_LINK_LIBRARIES( report libs... ) to add library deps.
    INCLUDE_DIRECTORIES( ${CMAKE_CURRENT_SOURCE_DIR}/.. )
    TARGET_LINK_LIBRARIES( test2200 orocos-apci ${OROCOS_RTT_LIBS})
    TARGET_LINK_LIBRARIES( test1032 orocos-apci ${OROCOS_RTT_LIBS})
    TARGET_LINK_LIBRARIES( test1710SSI orocos-apci ${OROCOS_RTT_LIBS})

ENDIF (BUILD_APCI_TEST)