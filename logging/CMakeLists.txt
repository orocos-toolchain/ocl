DEPENDENT_OPTION( BUILD_LOGGING "Build realtime Logging service" ON "BUILD_RTALLOC" OFF)

IF ( BUILD_LOGGING )

  FILE( GLOB CPPS [^.]*.cpp )
  FILE( GLOB HPPS [^.]*.hpp )

  INCLUDE_DIRECTORIES( "${CMAKE_SOURCE_DIR}/rtalloc" )

  INCLUDE_DIRECTORIES( "${LOG4CPP_INCLUDE_DIRS}" )
  LINK_DIRECTORIES( "${LOG4CPP_LIBRARY_DIRS}" )

  GLOBAL_ADD_COMPONENT( orocos-logging ${CPPS} )
  GLOBAL_ADD_INCLUDE( ocl ${HPPS})
  COMPONENT_ADD_LIBS( orocos-logging "${LOG4CPP_LIBRARIES}")

  ADD_SUBDIRECTORY( tests )

ENDIF ( BUILD_LOGGING )

